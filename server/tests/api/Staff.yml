info:
  name: Staff
  type: http
  seq: 6

http:
  method: GET
  url: http://192.168.4.254:5000/api/reports/summary?report=staff
  params:
    - name: report
      value: staff
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
