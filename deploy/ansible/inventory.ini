[primary]
primary ansible_host=127.0.0.1  # replace with primary tailscale ip

[standby]
standby ansible_host=127.0.0.1  # replace with standby tailscale ip

[all:vars]
ansible_user=ubuntu
ansible_python_interpreter=/usr/bin/python3
