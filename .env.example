# Environment template for Hospital Management System deployments
# Copy to /etc/hms.env (owned by root, mode 600) and fill secure values.

# PostgreSQL connection string (primary node)
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/hsp_db

# JWT signing key (generate with: openssl rand -hex 32)
JWT_SECRET_KEY=CHANGE_ME
